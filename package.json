{
  "name": "miaoxing",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf dist coverage",
    "test": "jest",
    "prepare": "yarn build",
    "lint": "eslint '*/**/*.{js,ts}'"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.5.0",
    "typescript": "^3.8.3"
  }
}
