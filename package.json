{
  "name": "miaoxing",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf dist coverage",
    "test": "jest",
    "prepare": "yarn build",
    "lint": "eslint '*/**/*.{js,ts}'"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
